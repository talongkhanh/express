<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button class="btn btn-primary">Học Ngay</button>
            <ul>
                <li>{{course.level}}</li>
            </ul>
        </div>
        <div class="col-lg-9">
            <h3>{{course.name}}</h3>
            <iframe width="100%" height="466px" src="https://www.youtube.com/embed/{{course.videoId}}" frameborder="0"
                allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"
                allowfullscreen></iframe>
            <p>{{course.desc}}</p>
        </div>
    </div>
</div>